using UnityEngine;
using System.Collections;

public class PlayerMovement : MonoBehaviour {
    /*
	public float moveSpeed = 100.0f;
	public float rotateSpeed = 5.0f;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.GetButton ("Forward")) {			//bevegelse forover
			Debug.Log ("Pressing w key");
			transform.Translate ((Vector3.forward) * moveSpeed * Time.deltaTime );
			//Debug.Log (Input.GetAxis ("Forward"));
		}

		if (Input.GetKey ("a")) {					//roter mot venstre
			transform.Rotate ((Vector3.down) * rotateSpeed * Time.deltaTime );
			Debug.Log ("ROTATING TO THE LEFT");
		}

		if (Input.GetKey ("d")) {					//roter mot høyre
			transform.Rotate ((Vector3.up) * rotateSpeed * Time.deltaTime );
			Debug.Log ("ROTATING TO THE RIGHT");
		}

		if (Input.GetButton ("Back")) {				// bevegelse bakover
			Debug.Log ("Pressing s key");
			transform.Translate ((Vector3.back) * moveSpeed * Time.deltaTime );
			//Debug.Log (Input.GetAxis ("Back"));
		}

		if (Input.GetKeyDown ("space")) {	//hopp
			//if (touchingGround = real) {
			//transform.Translate((Vector3.up * Time.deltaTime );
			//}
			Debug.Log ("JUMP");
		}

		if (Input.GetMouseButtonDown (0)) {	//venstre museknapp for abileties
			Debug.Log ("mouseLEFT");
		}

		if (Input.GetMouseButtonDown (1)) {	//høyre museknapp for abileties
			Debug.Log ("mouseRIGHT");
		}
	
	}*/
}
